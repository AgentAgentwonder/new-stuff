{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main:default",
  "description": "enables additional permissions for the main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
